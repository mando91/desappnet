@model IEnumerable<EmergenciasMX.Models.EmergenciaInstitucion>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.InstitucionPublica)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Emergencia)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.InstitucionPublica.Estado)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Emergencia.Descripcion)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.EmergenciaInstitucionId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.EmergenciaInstitucionId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.EmergenciaInstitucionId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
